// TypeScript(JavaScript)
// Jade(Html), Sass(CSS)
// 자바스크립트는
// 동작 구현(Action, Behavior),자바스트립트는 프론트엔드 웹환경에서는 HTML과 함께
// 백엔드 환경에서는 자바스크립트 혼자 동작 가능

// 행동1. 변수(Variable) 정의
var x = 10;; //이렇게 먼저 변수를 정의 해야한다.(선언)
var y = x + 9;

// 웹 브라우저 세상에는 콘솔이라는 불리는 객체(사물,Object)가 살고 있다.
// 콘솔(console). 이데이터를 참조한 변수를 화면에 기록(log)해줘.
// 콘솔.기록(변수);
console.log('x:', x); // x의 값을 기억하기 힘들기 때문에 레이블을 달아주는데 'x:'이런 형태로 레이블을 달기도 한다.
console.log('y:', y);

// 행동2. 선언(정의)된 변수 x에 값(데이터)을 할당(대입)
//x = 10; // 숫자(정수, 양의 정수)

// 선언과 동시에 값을 할당하는 경우가 많음

// 사칙연산

// x + 3; // 13
//  y = x + 9; // x값이 -12라면? y 값은?

// 문자 접합 (용접)
// 'This is ' + 'JavaScript'; // This is JavaScript

//
var laguage = 'this is '+'JavaScript'
console.log(laguage);

//
